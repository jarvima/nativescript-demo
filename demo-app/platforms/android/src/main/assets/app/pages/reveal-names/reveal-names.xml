<Page xmlns="http://schemas.nativescript.org/tns.xsd" xmlns:widgets="components" navigatingTo="onNavigatingTo" class="page">
  
    <StackLayout class="p-20">
        <widgets:header/>
        <Label text="Reveal names page" class="h2 text-center" textWrap="true"/>
        <Button text="Start Over" tap="{{ onTap }}" class="btn btn-primary btn-active"/>

    </StackLayout>
</Page>
