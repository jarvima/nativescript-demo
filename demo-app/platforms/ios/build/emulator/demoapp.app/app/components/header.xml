    <GridLayout columns="*, *, *">
        <Button text="Enter Names" tap="{{ showEnterNames }}" class="btn btn-primary btn-active"/>
        <Button text="Read Names" tap="{{ showReadNames }}" class="btn btn-primary btn-active"/>
        <Button text="Reveal Names" tap="{{ showRevealNames }}" class="btn btn-primary btn-active"/>
    </GridLayout>